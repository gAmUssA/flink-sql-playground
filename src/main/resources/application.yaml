server:
  port: 9090

spring:
  application:
    name: flink-sql-fiddle
  threads:
    virtual:
      enabled: true
  datasource:
    url: jdbc:h2:mem:fiddledb
    driver-class-name: org.h2.Driver
  jpa:
    hibernate:
      ddl-auto: validate

flink:
  parallelism: 1
  network-memory: 8m
  managed-memory: 32m
  max-sessions: 8
  session-idle-timeout: 3m

logging:
  level:
    com.flinksqlfiddle: DEBUG
    org.apache.flink: WARN
    org.hibernate: WARN
